// import 'package:fpdart/fpdart.dart';
// import 'package:location_app/core/errors/failures.dart';

/// Shared **type helpers and aliases**.
/// MUST be generic. NO feature-specific helpers allowed.

// Example if using a functional programming package like fpdart or dartz:
// typedef ResultFuture<T> = Future<Either<Failure, T>>;
// typedef ResultVoid = ResultFuture<void>;
// typedef ResultSync<T> = Either<Failure, T>;

/// Shortcut for standard JSON map typing
typedef DataMap = Map<String, dynamic>;
